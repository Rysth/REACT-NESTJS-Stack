# Mobile App Configuration

# API URL - For Docker backend running on port 3000
# The app will automatically detect the platform and use the correct URL

# Android Emulator:
# - Native Linux/Mac: Use 10.0.2.2
# - WSL2: Use WSL2 IP address (run: hostname -I | awk '{print $1}')
EXPO_PUBLIC_API_URL_ANDROID=http://10.0.2.2:3000

# iOS Simulator: localhost works directly
EXPO_PUBLIC_API_URL_IOS=http://localhost:3000

# Physical Device: Use your computer's local network IP
# Find your IP: 'ip addr show' (Linux/WSL2) or 'ipconfig getifaddr en0' (Mac)
EXPO_PUBLIC_API_URL_PHYSICAL=http://192.168.1.100:3000
